/**
 * Ask user to input string
*/
void askUserInput(char *);

/**
 * Ask user to input Password
*/
void askPassword(char *);

/**
 * Push message to Console
*/
void printMessage(char *, int);

/**
 * Checks if text is empty
*/
bool isEmpty(char *);

/**
 * Empty text
*/
void clean(char *);
bool equals(char *, char *);

/**
 * Concatinate username and password,
*/
void buildToken(char *, char *, char *);
